[package]
name = "indexwerk-core"
description = "Canonicalisation of index expressions. No input or output of any kind."
version.workspace = true
edition.workspace = true
repository.workspace = true
publish.workspace = true

# No dependencies. Every entry added here is a supply chain entry that M8 has to
# account for, so one is added only where an issue named it.
[dependencies]
